{
  "name": "uipe-workspace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": {
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r exec -- tsc --noEmit",
    "build:rust": "cargo build --release --bin uipe-vision",
    "lint": "pnpm -r lint"
  }
}
